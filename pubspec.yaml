name: consultant_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  get_storage: ^2.1.1
  dio: ^5.8.0+1
  country_flags: ^3.3.0
  go_router: ^15.2.0
  pinput: ^5.0.1
  otp_timer_button: ^1.1.1
  get_it: ^8.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
